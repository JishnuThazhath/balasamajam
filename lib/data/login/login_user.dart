/// Inputs username and password of the user to login
/// Talk to the database and returns if logged in or not
/// TODO: Ideally this method should return a session
bool loginUser(String username, String password) {
  return true;
}
